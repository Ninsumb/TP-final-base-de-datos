import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <h1>TP Final Base de Datos - Dashboard</h1>;

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(<App />);
